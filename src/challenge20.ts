type Reindeer = {
  type: string;
  weightCapacity: number;
};

type Gift = {
  country: string;
  weight: number;
};

export default function howManyReindeers(
  reindeerTypes: Reindeer[],
  gifts: Gift[]
) {
  return [];
}
